# ============================================
# SERVICE - Exposición de red
# ============================================
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: payment-service
  labels:
    app: payment-service

spec:
  # ========================================
  # Tipo de Service
  # ========================================
  type: NodePort

  # ========================================
  # Selector: ¿A qué pods dirigir tráfico?
  # ========================================
  selector:
    app: payment-service

  # ========================================
  # Puertos
  # ========================================
  ports:
    - name: http
      protocol: TCP

      # port: Puerto del SERVICE (interno)
      port: 80

      # targetPort: Puerto del CONTENEDOR
      targetPort: 8084

      # nodePort: Puerto en LOCALHOST
      # Rango: 30000-32767
      nodePort: 30084
